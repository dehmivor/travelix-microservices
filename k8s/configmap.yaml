apiVersion: v1
kind: ConfigMap
metadata:
  name: travelix-config
  namespace: travelix
data:
  DB_HOST: "postgresql-ha-postgresql"
  DB_PORT: "5432"
  DB_USER: "postgres"
  DB_NAME: "travelix"
  REDIS_HOST: "redis-cluster"
  REDIS_PORT: "6379"
  JWT_SECRET: "your-secret-key-change-in-production"
  NODE_ENV: "production"

