apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: travelix
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: your-dockerhub-username/travelix-user-service:latest
        ports:
        - containerPort: 3002
        envFrom:
        - configMapRef:
            name: travelix-config
        - secretRef:
            name: travelix-secrets
        env:
        - name: PORT
          value: "3002"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: travelix
spec:
  selector:
    app: user-service
  ports:
  - port: 80
    targetPort: 3002
  type: ClusterIP

